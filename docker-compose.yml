services:
  app:
    build: ./app
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    env_file: /etc/longlive/app.env
    environment:
      PORT: "3000"
    volumes:
      - /var/lib/longlive/state:/var/lib/longlive/state
